// =============================================================================
// filelist.f — VCS Gate-Level Simulation File List
//
// List all files required for VCS gate-level simulation here, one per line.
// This typically includes:
//   1. Standard cell simulation models (from your PDK)
//   2. Any analog model wrappers (RRAM behavioral models)
//
// Example:
//   /path/to/pdk/stdcell/verilog/stdcells.v
//   /path/to/pdk/memory/verilog/sram_model.v
//
// The synthesized netlist (reports/synth_netlist.v) and testbench
// (vcs/tb_top.sv) are passed directly on the VCS command line in the Makefile.
// =============================================================================

// TODO: Add your PDK standard cell model path(s) below:
// /path/to/your_pdk/stdcells.v
